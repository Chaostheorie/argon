import { RpcResponseError } from './interfaces';
import { RpcErrorCode } from './rpc-error-codes.enum';
export declare class RpcError<TError = any> extends Error {
    private readonly err;
    constructor(err: RpcResponseError<TError>);
    getCode(): RpcErrorCode | number;
}
